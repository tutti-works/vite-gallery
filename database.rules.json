{
  "rules": {
    "players": {
      ".read": true,
      ".write": true,
      "$playerId": {
        ".validate": "newData.hasChildren(['id', 'position', 'rotation', 'animation', 'timestamp'])"
      }
    }
  }
}